import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#c52f24",
    },
    secondary: {
      main: "#eeeeee",
    },
    background: {
      default: "#fff",
    },
    text: { primary: "#333333" },
  },
});

export default theme;
