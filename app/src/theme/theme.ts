import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#c52f24'
    },
    background: {
      default: '#ffeded',
    },
    text: { primary: '#333333' },
  },
});

export default theme;
